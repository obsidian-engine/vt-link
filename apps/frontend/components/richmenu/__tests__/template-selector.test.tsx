import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, it, expect, vi } from 'vitest'
// import { TemplateSelector } from '../template-selector'

/**
 * TemplateSelectorコンポーネントのテスト
 *
 * TODO: 以下のテストケースを実装してください：
 * 1. 3つのテンプレート（2分割、3分割、6分割）が表示される
 * 2. テンプレート選択時にonSelectコールバックが呼ばれる
 * 3. 選択中のテンプレートがハイライト表示される
 * 4. 各テンプレートのプレビュー画像が表示される
 */

describe('TemplateSelector', () => {
  it.todo('3つのテンプレートが表示される')

  it.todo('テンプレート選択時にonSelectコールバックが呼ばれる')

  it.todo('選択中のテンプレートがハイライト表示される')

  it.todo('各テンプレートのプレビュー画像が表示される')
})
