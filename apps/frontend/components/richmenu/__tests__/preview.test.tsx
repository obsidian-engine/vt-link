import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
// import { Preview } from '../preview'

/**
 * Previewコンポーネントのテスト
 *
 * TODO: 以下のテストケースを実装してください：
 * 1. 画像URLが渡された場合、画像が表示される
 * 2. エリアオーバーレイが画像上に表示される
 * 3. 各エリアの座標（bounds）が正しく適用される
 * 4. エリアをホバーした時にラベルが表示される
 * 5. 画像がない場合はプレースホルダーが表示される
 */

describe('Preview', () => {
  it.todo('画像URLが渡された場合、画像が表示される')

  it.todo('エリアオーバーレイが画像上に表示される')

  it.todo('各エリアの座標（bounds）が正しく適用される')

  it.todo('エリアをホバーした時にラベルが表示される')

  it.todo('画像がない場合はプレースホルダーが表示される')
})
