import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, it, expect, vi } from 'vitest'
// import { AreaEditor } from '../area-editor'

/**
 * AreaEditorコンポーネントのテスト
 *
 * TODO: 以下のテストケースを実装してください：
 * 1. ラベル入力フィールドが表示される
 * 2. URL入力フィールドが表示される（actionType="uri"の場合）
 * 3. テキスト入力フィールドが表示される（actionType="message"の場合）
 * 4. 入力値が変更された時にonChangeコールバックが呼ばれる
 * 5. 必須フィールドが空の場合にバリデーションエラーが表示される
 * 6. URLフォーマットが不正な場合にバリデーションエラーが表示される
 */

describe('AreaEditor', () => {
  it.todo('ラベル入力フィールドが表示される')

  it.todo('URL入力フィールドが表示される（actionType="uri"の場合）')

  it.todo('テキスト入力フィールドが表示される（actionType="message"の場合）')

  it.todo('入力値が変更された時にonChangeコールバックが呼ばれる')

  it.todo('必須フィールドが空の場合にバリデーションエラーが表示される')

  it.todo('URLフォーマットが不正な場合にバリデーションエラーが表示される')
})
