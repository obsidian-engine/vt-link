# vt-link Backend Environment Variables Template
# Copy this file to .env and fill in the actual values

# Database Configuration
# Get this from Neon Console → Settings → Connection Details
DATABASE_URL="postgresql://username:password@ep-endpoint.region.aws.neon.tech/vt_link_db?sslmode=require"

# Scheduler Authentication
# Generate with: openssl rand -hex 32
# This MUST match the value in Cloudflare Workers
SCHEDULER_SECRET="your-256-bit-random-secret-key-here"

# LINE Messaging API
# Get this from LINE Developer Console → Your Channel → Channel access token
LINE_CHANNEL_ACCESS_TOKEN="your-line-channel-access-token-here"

# Application Settings
NODE_ENV="development"
VT_LINK_VERSION="dev"

# Optional: Debug Settings (development only)
# LOG_LEVEL="debug"
# DISABLE_AUTH="true"